import { Svg } from "../../app/shared/svg";

export const Icons = {
  apple: Svg(require('./apple.svg')),
  google: Svg(require('./google.svg')),
  facebook: Svg(require('./facebook.svg')),
  logo: Svg(require('./logo.svg'))
}
